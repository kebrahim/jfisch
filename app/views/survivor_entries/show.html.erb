<%= navigationBar(NavigationHelper::MY_ENTRIES_BUTTON) %>

<div class='row-fluid'>
  <div class='span12 center'>
    <h3><%= @user.first_name %>'s <%= @survivor_entry.type_title %> Entry</h3>
  </div>
</div>

<div class='row-fluid'>
  <div class='span7 center'>
    <%= show_notice_as_alert(notice) %>
    <form action='/save_entry' method='post'>
      <h4><%= @survivor_entry.type_title %> Picks</h4>
      <%= entry_bets_table(@survivor_bets, @team_to_games_map, @nfl_teams) %>
      <!-- TODO button to update -->
    </form>
  </div>

  <div class='span5 center'>
    <h4>NFL Schedule</h4>
    <!-- TODO weekly schedule w/ week dropdown that loads async -->
  </div>
</div>

</form>
