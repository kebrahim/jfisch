
<%= navigationBar(NavigationHelper::RULES_BUTTON_MAP[@game_type]) %>

<div class='row-fluid'>
  <div class='span3 center nexttoh1'>
    <a href=<%= "'/" + @game_type.to_s + "'" %> class='btn btn-inverse'>Entry Breakdown</a>
  </div>
  <div class='span6 center'>
    <h3>NFL <%= SurvivorEntry.game_type_title(@game_type) %></h3>
  </div>
  <div class='span3 center nexttoh1'>
    <a href=<%= "'/" + @game_type.to_s + "/week'" %> class='btn btn-inverse'>Weekly Breakdown</a>
  </div>
</div>

<div class='row-fluid'>
  <div class='span12'>
    <h4><%= SurvivorEntry.game_type_title(@game_type) %> Rules</h4>
    <%= render @game_type.to_s %>
  </div>
</div>
